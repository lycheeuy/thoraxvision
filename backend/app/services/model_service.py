"""Model service (Phase 3+).

Responsibility: model lifecycle use cases — load metadata via
model_repository, report model readiness, and select the active model
version. Does not perform inference (that lives in app.ml.model).
"""
