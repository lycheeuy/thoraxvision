import { History } from "lucide-react";

import { PlaceholderPage } from "@/components/common/placeholder-page";

export default function HistoryPage() {
  return (
    <PlaceholderPage
      icon={History}
      title="History"
      description="Past predictions with thumbnails, labels, and confidence scores."
      phase="Phase 8"
    />
  );
}