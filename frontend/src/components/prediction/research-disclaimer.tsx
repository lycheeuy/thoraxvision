/**
 * Compact research disclaimer.
 *
 * Present on the workspace whenever a result is shown. Deliberately quiet —
 * an info panel, not a warning banner — but always there: this is a research
 * decision-support tool, not a diagnosis.
 */
import { Info } from "lucide-react";

import { cn } from "@/lib/utils";

export function ResearchDisclaimer({ className }: { className?: string }) {
  return (
    <div
      className={cn(
        "flex items-start gap-3 rounded-xl border border-border/70 bg-muted/40 px-4 py-3",
        className,
      )}
    >
      <Info className="mt-0.5 h-4 w-4 shrink-0 text-muted-foreground" aria-hidden />
      <p className="text-xs leading-relaxed text-muted-foreground">
        <span className="font-medium text-foreground">Research decision-support only.</span>{" "}
        This analysis is generated by an experimental model and is not a clinical
        diagnosis. All findings must be reviewed and confirmed by a qualified radiologist.
      </p>
    </div>
  );
}